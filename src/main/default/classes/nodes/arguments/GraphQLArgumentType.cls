/**
 * @description The list of all available types for GraphQL arguments
 */
global enum GraphQLArgumentType {
    x_Integer,
    x_Float,
    x_String,
    x_Boolean,
    x_DateTime,
    x_Date,
    x_Null,
    x_Object,
    x_Enum
}
