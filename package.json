{
  "name": "graphql-apex-client",
  "private": true,
  "version": "1.0.0",
  "description": "graphql-apex-client",
  "scripts": {
    "prettier": "prettier --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^0.12.0",
    "husky": "^4.2.1",
    "lint-staged": "^10.0.7",
    "prettier": "^2.0.5",
    "prettier-plugin-apex": "^1.8.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "**/*.{cls,trigger,md,xml,yaml,yml,json}": [
      "prettier"
    ]
  }
}
